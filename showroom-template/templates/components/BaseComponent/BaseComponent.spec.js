import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import __jcatalog-react-app_component-name--uppercamel-case__ from '.';

describe('<__jcatalog-react-app_component-name--uppercamel-case__ />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <__jcatalog-react-app_component-name--uppercamel-case__ />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<__jcatalog-react-app_component-name--uppercamel-case__ className="test-class-name" />);
    expect(wrapper).to.have.className('__jcatalog-react-app_component-name--kebab-case__');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
